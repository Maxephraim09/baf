@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-6xl px-4 py-16 sm:px-6 lg:px-8"><a class="text-sm font-semibold" style="color:var(--primary)" href="{{ route('home') }}">&larr; Back to home</a><h1 class="mt-6 text-4xl font-bold" style="color:var(--secondary)">Cultural Heritage</h1><p class="mt-3 text-gray-600">Explore traditional artifacts, media, archives, and educational resources.</p><form class="mt-8 grid gap-3 sm:grid-cols-[1fr_220px_auto]"><input class="rounded-lg border-gray-300" name="q" value="{{ request('q') }}" placeholder="Search heritage"><select class="rounded-lg border-gray-300" name="type"><option value="">All resources</option>@foreach(['artifact','video','audio','photo','educational_resource','historical_archive'] as $type)<option value="{{ $type }}" @selected(request('type') === $type)>{{ str_replace('_', ' ', ucfirst($type)) }}</option>@endforeach</select><button class="rounded-lg px-5 py-2 font-bold text-white" style="background:var(--primary)">Search</button></form><div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">@forelse($items as $item)<a href="{{ route('heritage.show', $item) }}" class="rounded-xl bg-white p-6 shadow"><p class="text-xs font-bold uppercase" style="color:var(--primary)">{{ str_replace('_', ' ', $item->type) }}</p><h2 class="mt-2 text-xl font-bold" style="color:var(--secondary)">{{ $item->title }}</h2><p class="mt-3 text-gray-600">{{ $item->description }}</p></a>@empty<p class="text-gray-600">No heritage resources match your search.</p>@endforelse</div><div class="mt-8">{{ $items->links() }}</div></div>
@endsection
